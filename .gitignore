__pycache__/
*.pyc
.DS_Store

# Render outputs: regenerated by running the scripts, they do not belong in git.
# If a render is a deliverable, move it to assets/ and commit it by hand.
# renders/* not renders/: git cannot re-include a file whose directory is excluded
renders/*
!renders/.gitkeep

# ...except the city, which IS the deliverable rather than a by-product. 19 MB,
# and that is everything repeated being an instance rather than a copy.
!renders/city.blend
!renders/city_final.png

# AND THE FOUR FILES THAT TRAVEL WITH IT. They are not renders: they are what
# the .blend cannot say about itself — the lot table, the rectangle every solid
# thing occupies, the sign manifest, and which wings make up each building. Ten
# scripts read each one.
#
# Only city_lots.json used to be here, so a fresh clone had a 19 MB city and
# could not run a single check against it: 93 died on a FileNotFoundError
# naming a file nothing tells you how to produce. 119 KB against the .blend's
# 19 MB is not the trade it looked like.
!renders/city_lots.json
!renders/city_solids.json
!renders/city_signs.json
!renders/city_buildings.json

# Reference frames from third-party footage. Study material, not ours to
# redistribute. Kept locally so the look can be compared side by side.
refs/

# Blender
*.blend1
*.blend@

# .agents/skills/ IS tracked: .claude/skills/threejs-* are symlinks into it, so
# a clone without those files would end up with broken skills.

# Harness scratch, not project content.
tasks/

# The web build. Everything under web/public/ except index-level assets is
# generated: city.* by ./bl scripts/city/20_export_web.py, draco/ by npm.
# The .glb is the .blend exported, so committing it would be committing the
# same city twice, and the two copies would drift.
web/node_modules/
web/dist/
web/.vite/
web/public/city.glb
web/public/city_motion.json
web/public/city_shot.json
web/public/city_spots.json
web/public/city_sky.exr
web/public/draco/
